import DefaultRenderer from './default'

class VisionariesLandingRenderer extends DefaultRenderer {
    onEnter() {}
    onLeave() {}
    onEnterCompleted() {
      super.onEnterCompleted()
    }
    onLeaveCompleted() {
      super.onLeaveCompleted()
    }
}

export default VisionariesLandingRenderer;